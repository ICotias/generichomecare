import { SkeletonScreen } from '../../../shared/components/SkeletonScreen';

export const RegisterActivityScreen = () => (
  <SkeletonScreen title="Atividades" subtitle="Banho, higiene, curativo, mobilidade" />
);
